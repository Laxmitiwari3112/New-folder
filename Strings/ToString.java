package Strings;
import java.util.*;

//to string method of string
public class ToString {
    public static void main(String[] args) {

        //input
        int number=123;
        String str=Integer.toString(number);

        //output (no of integer use=?)
        System.out.println(str.length());
    }
}
